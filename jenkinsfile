pipeline {
    agent { docker 'maven:3-alpine' } 
    stages {
        stage('Build') {
            steps {
                echo 'Building projects'
            }
        }
        stage('Test') {
            steps {
                echo 'Testng projects'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying projects'
            }
        }
    }
}
